# HTTP-only + ACME. setup_ssl.sh copies this to default.conf before Certbot.

server {
    listen 80;
    server_name _;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
