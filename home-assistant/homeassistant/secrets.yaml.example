# Copy to secrets.yaml and fill in. Never commit secrets.yaml.

# Must match your public URL (DuckDNS). Same subdomain as DUCKDNS_SUBDOMAIN in .env.
ha_external_url: "https://your-subdomain.duckdns.org"
