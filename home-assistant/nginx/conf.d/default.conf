# Active site config. Before SSL: same as default-http.conf. After setup_ssl.sh: TLS config.

server {
    listen 80;
    server_name _;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
