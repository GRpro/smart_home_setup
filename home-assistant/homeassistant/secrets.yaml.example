# ─────────────────────────────────────────────────────────────────
#  secrets.yaml.example — copy to secrets.yaml on the VPS and fill
#  in real values. NEVER commit secrets.yaml to git.
# ─────────────────────────────────────────────────────────────────

# Home Assistant currently connects to Mosquitto anonymously.
# If you decide to add MQTT credentials later, you can store 
# them here and reference them in configuration.yaml.
